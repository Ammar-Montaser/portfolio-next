import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "i.postimg.cc",
      "iili.io",
      "securestream.tech",
      "jobs.teleperformance.es",
      "images.wuzzuf-data.net",
      "firebasestorage.googleapis.com",
    ],
  },
  /* config options here */
};

export default nextConfig;
